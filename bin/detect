#!/usr/bin/env bash

if [ -f $1/app/server/nginx/nginx.conf.erb && -f $1/app/server/nginx/project.conf.erb && -f $1/app/server/php/php.ini && -f $1/app/server/php-fpm/php-fpm.conf ]; then
	echo "Nginx/PHP"
	exit 0
else 
	echo "No"
	exit 1
fi