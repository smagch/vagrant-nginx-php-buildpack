#!/usr/bin/env bash

#if [ -f $1/_config/php.ini && -f $1/_config/php-fpm.ini && -f $1/_config/nginx.conf.erb ]; then
if [ -f $1/app/server/nginx/nginx.conf.erb && $1/app/server/nginx/project.conf.erb && $1/app/server/php/php.ini && $1/app/server/php-fpm/php-fpm.conf ]; then
	echo "Nginx/PHP"
	exit 0
else 
	echo "No"
	exit 1
fi